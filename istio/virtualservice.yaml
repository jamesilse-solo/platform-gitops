apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: node-app-vs
spec:
  hosts:
  - "*"
  gateways:
  - nginx-gateway
  http:
  - route:
    - destination:
        host: node-app.default.svc.cluster.local
        port:
          number: 80
