apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: nginx-gateway
spec:
  selector:
    app: istio-ingressgateway   # ✅ FIXED
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
