apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentgateway-oss-chart.fullname" . }}
  labels:
    {{- include "agentgateway-oss-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentgateway-oss-chart.selectorLabels" . | nindent 4 }}
